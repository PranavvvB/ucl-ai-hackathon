import { LiftPage } from "@/components/LiftPage";

export default function LiftsPage() {
  return (
    <LiftPage
      liftType="general"
      title="Analyze Your Lift"
      description="Upload any powerlifting video — squat, bench press, or deadlift — and get instant AI feedback."
    />
  );
}
